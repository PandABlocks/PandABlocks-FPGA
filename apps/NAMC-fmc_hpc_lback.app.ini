[.]
description:
    Standard set of NAMC blocks with:
        - FMC HPC loopback module
    Used for NAMC hardware testing only 
target: NAMC
includes: common_soft_blocks.include.ini

[FMC_LBACK]
module: fmc_hpc_loopback
