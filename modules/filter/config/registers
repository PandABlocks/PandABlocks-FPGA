FILTER        {{newBlockNo("FILTER")}}
    MODE            {{newBlockReg()}}
    TRIG            {{newBlockReg(typ='bit_mux')}}
    INP             {{newPosBus(location='upper')}}
    ENABLE          {{newBlockReg(typ='bit_mux')}}
    OUT             {{newPosBus(location='upper')}}
    READY           {{newBitBus()}}
    ERR             {{newBlockReg()}}