FILTER[{{app_config["FILTER"]}}]
    MODE            param       enum 2
        0   difference
        1   average
    TRIG            bit_mux
    INP             pos_mux
    ENABLE          bit_mux
    OUT             pos_out
    READY           bit_out
    ERR             read        enum 3
        0   OK
        1   Accumulator overflow
        2   Divider retrigger
       
