POSENC      {{newBlockNo("POSENC")}}
    INP             {{newBlockReg()}}
    QPERIOD         {{newBlockReg()}}
    ENABLE          {{newBlockReg(type='bit_mux')}}
    PROTOCOL        {{newBlockReg()}}
    A               {{newBitBus()}}
    B               {{newBitBus()}}
    QSTATE          {{newBlockReg()}}


