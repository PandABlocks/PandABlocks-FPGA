POSENC      {{newBlockNo("POSENC")}}
    INP             {{newBlockReg()}}
    QPERIOD         {{newBlockReg()}}
    ENABLE          {{newBlockReg(typ='bit_mux')}}
    PROTOCOL        {{newBlockReg()}}
    A               {{newBitBus()}}
    B               {{newBitBus()}}
    QSTATE          {{newBlockReg()}}


