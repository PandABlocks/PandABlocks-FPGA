PULSE       {{newBlockNo("PULSE")}}
    DELAY           {{newBlockReg(typ='time')}}     >3
    WIDTH           {{newBlockReg(typ='time')}}     >3
    INP             {{newBlockReg(typ='bit_mux')}}
    ENABLE          {{newBlockReg(typ='bit_mux')}}
    OUT             {{newBitBus()}}
    ERR_OVERFLOW    {{newBlockReg()}}
    ERR_PERIOD      {{newBlockReg()}}
    QUEUE           {{newBlockReg()}}
    MISSED_CNT      {{newBlockReg()}}
    TRIG_EDGE       {{newBlockReg()}}


