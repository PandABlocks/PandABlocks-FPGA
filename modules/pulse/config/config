PULSE[{{app_config["PULSE"]}}]
    DELAY           time
    WIDTH           time
    TRIG            bit_mux
    ENABLE          bit_mux
    OUT             bit_out
    QUEUED          read        uint 1023
    DROPPED         read
    TRIG_EDGE       param       enum
        0   Rising
        1   Falling
        2   Either


