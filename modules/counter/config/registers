COUNTER     {{newBlockNo("COUNTER")}}
    START           {{newBlockReg()}}
    STEP            {{newBlockReg()}}
    MAX             {{newBlockReg()}}
    MIN             {{newBlockReg()}}
    ENABLE          {{newBlockReg(typ='bit_mux')}}
    TRIG            {{newBlockReg(typ='bit_mux')}}
    DIR             {{newBlockReg(typ='bit_mux')}}
    CARRY           {{newBitBus()}}
    OUT             {{newPosBus()}}


