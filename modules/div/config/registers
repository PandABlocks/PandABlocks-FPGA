DIV         {{newBlockNo("DIV")}}
    DIVISOR         {{newBlockReg()}}
    FIRST_PULSE     {{newBlockReg()}}
    INP             {{newBlockReg(typ='bit_mux')}}
    ENABLE          {{newBlockReg(typ='bit_mux')}}
    OUTD            {{newBitBus()}}
    OUTN            {{newBitBus()}}
    COUNT           {{newBlockReg()}}


