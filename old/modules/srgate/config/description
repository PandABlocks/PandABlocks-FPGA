SRGATE      Set reset gate
    WHEN_DISABLED   What to do with the output when Enable is low
    SET_EDGE        Output set edge (0 = rising edge, 1 = falling edge, 2 = Rising or Falling)
    RST_EDGE        Output reset edge (0 = rising edge, 1 = falling edge, 2 = Rising or Falling)
    FORCE_SET       Set output to 1
    FORCE_RST       Reset output to 0
    ENABLE          Whether to listen to SET/RST events
    SET             A falling/rising edge sets the output to 1
    RST             A falling/rising edge resets the output to 0
    OUT             Output value
